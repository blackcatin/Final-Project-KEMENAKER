{
    "build": {
      "env": {
        "NPM_CONFIG_QUIET": "true",
        "NPM_CONFIG_PROGRESS": "false"
      },
      "commands": {
         "build": "chmod +x node_modules/.bin/vite && npm run build"
      }
    }
  }
  